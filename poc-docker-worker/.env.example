# Generate this token by running: claude setup-token
# This creates a 1-year OAuth token that bills to your Max plan.
# NEVER commit this file with a real token — use .env (which is gitignored).
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-your-token-here
